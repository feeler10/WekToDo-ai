# P0 与 P1a 开发计划

## 1. 范围判定

本计划以 `docs/PROJECT_TASK_BOOK.md` 第 23 章 P0 清单和第 29.1 节第一版验收标准为准。第一版目标是形成以下最小闭环：

```text
自然语言输入 → 结构化解析 → 时间/优先级计算 → 用户确认
→ Redis 保存 → 对话查询 → 状态更新 → Checkpoint 恢复
```

P0 包含 FastAPI 服务、单一 LangGraph 主流程、Agent State、意图识别、任务结构化解析、时间解析、优先级计算、Human-in-the-loop、Redis Checkpoint、创建/查询/状态更新工具、幂等控制和基础对话页。第 29.1 节要求的用户隔离、真实数据查询和最小工具执行日志属于闭环验收约束；这不等同于提前实现 P1 的完整 Trace。

P0 闭环完成后进入 P1a：实现 AI 任务拆解、确定性方案校验、确认式原子批量创建直接子任务，以及子任务状态对父任务进度和完成状态的确定性联动。P1a 不引入 Milvus、历史拆解检索、动态重新规划、跨父任务依赖或多层任务树。

## 2. 分阶段实施与独立验收

### 阶段 0：范围与契约冻结（当前）

明确 P0、目录边界、数据字段、状态流转、确认规则和延期项。验收：`DEVELOPMENT_PLAN.md` 与 `AGENTS.md` 内容一致，未产生业务代码或依赖变更。

### 阶段 1：FastAPI 基础骨架

初始化 Python 3.11+ / FastAPI 工程、统一配置、统一异常处理和健康检查，建立 pytest 测试骨架并补充启动文档。验收：服务可启动；`GET /health` 返回 `status=ok`；health 测试可从干净检出重复运行。此阶段不接入 Redis、LangGraph、大模型、任务业务或前端。

### 阶段 2：确定性领域与 Redis 存储

接入 Redis 连接与健康检查；定义 Task、PendingAction、工具参数等 Pydantic Schema；实现时区时间解析、优先级公式、合法状态流转、任务存储、`user_id` 隔离、版本校验和幂等键。验收：单元测试覆盖正常、边界、歧义、非法流转、跨用户访问与重复请求，任务读写不依赖 LLM。

### 阶段 3：只读 Agent 与任务草稿

定义可序列化 Agent State，完成 `load_context`、`classify_intent`、`parse_task`、`validate_task`、`calculate_priority`、`query_tasks` 和错误处理节点。验收：自然语言可生成结构化待确认草稿；信息不足时不写入；查询从 Redis 返回真实数据；节点和条件路由可独立测试。

### 阶段 4：确认式任务创建

实现 PendingAction、`prepare_tool_call`、Interrupt/Resume、确认/编辑/重生成/取消分支、Redis Checkpoint 与 `create_task`。验收：未确认不写入；恢复从 Checkpoint 继续而非重跑；相同幂等键只创建一次；重启会话后可查询已创建任务。

### 阶段 5：状态更新与 API 闭环

实现任务引用解析、候选消歧、确认式状态更新、线程状态查询及最小工具审计日志，完成 chat/confirm/resume 和必要任务查询接口。验收：创建、查询、状态更新、拒绝、异常恢复和用户隔离通过端到端测试；非法状态流转、版本冲突和 Redis 故障不会继续写入。

### 阶段 6：P0 基础对话页与交付

后端闭环稳定后再创建最小 Vue 3 + TypeScript 页面，仅支持对话输入、草稿展示、确认操作和结果查看；不建设复杂任务面板。验收：用户可从页面完成 P0 端到端闭环；部署与使用文档包含可复现命令。注意：本次文档任务不创建任何前端文件。

## 3. 阶段 1 预计文件清单

阶段 1 实施时预计创建：

```text
agent-service/
├── pyproject.toml
├── app/
│   ├── __init__.py
│   ├── main.py
│   ├── api/
│   │   ├── __init__.py
│   │   ├── router.py
│   │   └── health.py
│   ├── core/
│   │   ├── __init__.py
│   │   ├── config.py
│   │   └── exceptions.py
└── tests/
    ├── __init__.py
    ├── conftest.py
    └── test_health.py
```

同时创建 `.gitignore`，更新现有 `.env.example` 和 `README.md`。阶段 1 不创建 `frontend/` 内容，也不创建 Redis、Graph、任务工具或领域服务实现。


## 3.1 v0.1.2 查询上下文闭环

阶段三复用现有 Redis Checkpointer 保存短期 `PendingQueryClarification` 与 `PendingTaskSelection`。查询补丁按字段确定性合并并重新执行阶段一完整性校验；多候选只保存候选 ID、版本和最小操作上下文，选择后按 `user_id + task_id` 重读 Redis。查询选择只读，状态更新选择后继续进入现有 PendingAction 确认链路。所有待处理状态绑定用户和线程、统一过期，并在完成、取消、新请求替换或不可恢复错误时清理。中文响应只格式化真实结构化数据，不查询仓储或调用 LLM。

## 3.2 v0.1.3 对话式任务属性修改

在现有 `UPDATE_TASK` 意图、关键词任务匹配、PendingAction 与 Redis 乐观并发控制基础上，增加对话式普通属性修改闭环。第一阶段仅允许修改 `title`、`description`、`category`、`deadline`、`estimated_minutes` 和 `user_priority`；状态继续由 `UPDATE_TASK_STATUS` 独立处理，禁止修改 ID、归属、AI 优先级、有效优先级、紧急度、进度、版本和审计时间字段。

属性修改采用独立结构化解析器：意图识别只确定 `UPDATE_TASK` 和任务引用，从 Redis 唯一定位任务后，解析器结合当前任务、业务时区和当前时间生成最小字段操作列表。多候选状态保留原始修改语句，用户选择后重新按 `user_id + task_id` 读取并校验版本。所有补丁经过 Pydantic 白名单校验、前后值预览和 Interrupt 确认，再使用 `WATCH/MULTI/EXEC`、`expected_version` 与 `update_task:{request_id}` 幂等键写入 Redis。

验收覆盖标题、截止时间、优先级、多字段修改、字段清空、解析澄清、多候选恢复、拒绝、重复请求、版本冲突和用户隔离；基础 Vue 确认卡只允许确认或取消属性修改。

## 3.3 v0.2.0 P1a 任务拆解与原子批量子任务

在现有 `DECOMPOSE_TASK` 意图、任务引用解析、候选消歧、PendingAction、Interrupt/Resume 和 Redis 乐观并发控制基础上，增加任务拆解闭环：读取真实父任务及其已有子任务，使用独立结构化解析器生成 3—8 个候选子任务，再由确定性服务检查标题与步骤标识唯一性、顺序、依赖引用、依赖环、截止时间、预计耗时和重复子任务。LLM 不得生成或覆盖 `user_id`、`parent_id`、初始状态、版本和审计字段。

所有拆解结果必须作为所选父任务的直接子任务保存并携带 `parent_id`。拆解方案支持确认、编辑后确认、重新生成和取消；只有确认后才能调用仓储级 `create_subtasks_batch`。批量创建使用单个 Redis `WATCH/MULTI/EXEC` 事务原子写入全部子任务、用户任务索引、父任务子节点有序索引、父任务版本和批次幂等结果，禁止循环调用单任务创建造成部分成功。确认时必须重新校验父任务的 `user_id`、版本与状态；相同幂等键仅能重放完全相同的批次结果。

父任务进度采用有效子任务数量计算，`CANCELLED` 不计入有效子任务。子任务状态更新必须与父任务汇总处于同一个 Redis 事务：存在至少一个有效子任务且全部为 `DONE` 时，父任务自动更新为 `DONE`、`progress=100` 并写入 `completed_at`；已完成子任务重新打开或新增有效子任务时，父任务自动恢复为 `DOING`、重新计算进度并清空 `completed_at`。该联动属于已确认子任务写操作的确定性副作用，不重复请求确认。

对话式普通任务列表按直接父子关系汇总，只逐项输出顶层任务，并在父任务标题后标注直接子任务数量；子任务不得作为同级任务重复展开。用户明确询问某个父任务的子任务数量、列表或具体步骤时，查询意图必须保留该语义，完成父任务唯一定位或候选消歧后，再从 Redis 子节点索引读取并按顺序返回真实直接子任务。

验收覆盖任务归属与候选消歧、结构化解析失败、重复步骤、非法依赖和循环依赖、未确认零写入、批次全有或全无、重复确认、幂等键冲突、父任务版本冲突、用户隔离、并发完成最后多个子任务、父任务自动完成、子任务重新打开导致父任务恢复，以及 Checkpoint 中断恢复。

## 3.4 v0.2.1 对话式任务删除

在现有任务引用解析、候选消歧、PendingAction、Interrupt/Resume 和 Redis 乐观并发控制基础上，增加独立的 `DELETE_TASK` 意图与确认式物理删除闭环。删除与 `CANCELLED` 状态严格分离：取消任务继续走状态更新，删除任务必须唯一定位真实 Redis 任务、展示不可恢复警告，并在用户确认后调用 `delete_task`。删除确认只支持确认或拒绝；未确认、拒绝、任务版本变化或归属校验失败时保持零写入。

第一阶段只允许删除叶子任务，不执行隐式级联删除。存在直接子任务的任务拒绝删除；仍被同一父任务下其他子任务依赖的子任务也拒绝删除。删除子任务时，目标任务对象、用户任务索引、父任务子节点有序索引、父任务进度/状态/完成时间/版本以及删除幂等结果必须在同一个 Redis `WATCH/MULTI/EXEC` 事务中全有或全无地更新。剩余有效子任务按数量重新汇总，`CANCELLED` 不计入有效数量；有效子任务为空时父任务不得因此保持自动完成状态。

验收覆盖删除与取消/属性修改/查询的意图边界、任务缺失澄清、候选消歧、未确认零写入、拒绝、叶子任务删除、父任务与依赖阻塞、子任务索引清理、父任务确定性联动、重复确认、幂等重放与冲突、版本冲突、用户隔离、事务回滚、Checkpoint 中断恢复和基础 Vue 危险操作确认卡。真实 Redis 与 Checkpoint 集成验证统一使用 IPv6 回环地址 `[::1]`，不依赖未打通的 IPv4 回环连接。

## 3.5 v0.2.2 自然语言删除计划与已取消任务恢复

`DELETE_TASK` 只负责识别删除类操作，具体范围由独立结构化 LLM 解析器转换为受控查询参数，包括任意明确自然语言时间范围、状态、优先级、分类、关键词、单个或多个任务引用以及单项/全部匹配语义。后端必须确定性校验解析结果，再从 Redis 查询真实任务并生成完整删除预览；LLM 不得接触任务真实数据、生成任务 ID、跳过确认或直接执行删除。

单项和批量删除统一使用删除计划：向用户展示全部匹配任务、状态、截止时间和阻塞原因，确认后在单个 Redis `WATCH/MULTI/EXEC` 事务中删除全部目标并更新受影响父任务。批次限制为 50 项，禁止静默截断和部分成功；存在未纳入本批次的直接子任务或外部依赖时整批拒绝确认。模糊关键词匹配作用于标题、描述和分类，并始终限制当前 `user_id`。

`CANCELLED` 继续表示保留数据的终止状态。查询已取消任务时明确提示可恢复；属性修改、状态修改或拆解命中已取消任务时，先生成独立恢复确认，将任务恢复为 `TODO`，确认成功后自动回到原请求并继续原有解析/确认链路。删除已取消任务不要求先恢复。恢复、后续修改和删除分别使用独立幂等键，任何一步失败都不得越过后续确认。

验收覆盖任意时间表达解析、状态/关键词/多引用组合、空结果、超过批次上限、模糊匹配预览、未确认零写入、批量全有或全无、父子与依赖阻塞、用户隔离、版本及幂等冲突、恢复前置确认、恢复后原意图续接，以及 IPv6 `[::1]` 下真实 Redis/Checkpoint 恢复。

删除范围协议支持 `ALL_TASKS` 与 `DIRECT_CHILDREN`；后者必须携带父任务引用，先唯一定位父任务，再限定其直接子任务。多个显式任务引用必须逐项得到唯一结果：任何引用缺失时禁止生成部分删除计划，多个候选时通过可持久化选择上下文逐项消歧。

## 4. 当前明确不实现

- **P1 后续项**：下一步推荐、SSE、完整工具日志、基础 Trace、跨父任务依赖、多层任务树和已有执行计划覆盖。
- **P2**：Milvus、Embedding 与长期记忆、Reranker、混合检索、每日简报、动态重新规划、周期复盘、完整评测平台、复杂任务面板。
- **复杂前端**：任务看板、每日简报页、执行轨迹页、统计图表、复杂筛选、主题系统、移动端适配；P0 只保留基础对话和确认交互。
- **任务书明确延期项**：多 Agent、MCP Server、邮件/日历自动操作、语音、多人协作、移动 App、复杂权限、复杂 RAG/甘特图、自动长期日程、团队/社交功能及高风险外部操作。

任何延期项进入实施前，必须先更新范围、验收标准和本计划；不得以“预留”为由提前引入依赖或业务代码。
